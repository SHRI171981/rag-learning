{
    "title": "AnnotatedReview",
    "type": "object",
    "properties": {
        "summary": {
            "type": "string",
            "description": "A brief summary of the movie review."
        },
        "sentiment": {
            "type": "string",
            "enum": ["Good", "Bad", "Ok"],
            "description": "The overall sentiment of the review."
        },
        "key_themes": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "A list of key themes mentioned in the review."
        },
        "pros": {
            "type": ["array", "null"],
            "items": {
                "type": "string"
            },
            "description": "A list of pros mentioned in the review. Can be null if not mentioned."
        },
        "cons": {
            "type": ["array", "null"],
            "items": {
                "type": "string"
            },
            "description": "A list of cons mentioned in the review. Can be null if not mentioned."
        }
    },
    "required": ["summary", "sentiment", "key_themes", "pros", "cons"]
}